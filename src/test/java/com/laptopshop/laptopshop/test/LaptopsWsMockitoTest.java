package com.laptopshop.laptopshop.test;

import java.util.ArrayList;

import com.laptopshop.laptopshop.model.Laptop;

public class LaptopsWsMockitoTest {
	// private LaptopWS myLapWs = mock(LaptopWS.class);
	private ArrayList<Laptop> laptopList;

	// @Before
	// public void setUp() throws Exception {
	// pas = mock(PublicAddressSystem.class);
	// bazar = new BiggestBazarRetail(myInv, pas);
	// expiredList = new ArrayList<Item>();
	// }
	//
	// @Test
	// public void TestThatADiscountIsIssuedOnOneItem() {
	// Item soap = new Item("123", "Luxury", SOAP_COST_PRICE, SOAP_BASE_PRICE);
	// expiredList.add(soap);
	// // Stubs
	// when(myInv.getItemsExpireInAMonth()).thenReturn(expiredList);
	// when(myInv.itemsUpdated()).thenReturn(1);
	// // test
	// int offers = bazar.issueDiscountForItemsExpireIn30Days(DISCOUNT_RATE);
	// assertEquals(1, offers);
	// verify(myInv, new Times(1)).update(soap, 70.00);
	// verify(pas).announce(isA(Offer.class));
	//
	// }

}
